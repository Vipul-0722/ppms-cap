replicaCount: 1

image:
  repository: vipul710/my-bookshop
  pullPolicy: IfNotPresent
  tag: "latest"

service:
  type: LoadBalancer
  port: 4004

# Note: Database and XSUAA credentials should be managed via Kubernetes secrets
# and referenced in the deployment. This example assumes they are set as environment variables.
# For example, you would create a secret with your HANA credentials and mount it.
# env:
#   - name: "VCAP_SERVICES"
#     valueFrom:
#       secretKeyRef:
#         name: hana-credentials
#         key: VCAP_SERVICES